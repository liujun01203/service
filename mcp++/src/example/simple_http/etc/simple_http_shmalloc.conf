<root>
	#共享内存key，默认287454028
	shmkey = 287954088
	#共享内存大小，默认128MB，这里共享内存大小至少要比minchunksize*maxchunknum大一些
	shmsize = 134217728
	#posix信号量名字，用于多进程互斥
	semname = /simple_http_shmalloc
	#最小块大小，默认64KB
	minchunksize = 1024
	#最大块大小，默认2MB
	maxchunksize = 131072
	#最大块数，默认2000
	maxchunknum = 2000
</root>

